name: CI

on: [push, pull_request]

env:
  DATABASE_URL: "asdf"
  UPSTASH_REDIS_REST_URL: "asdf"
  UPSTASH_REDIS_REST_TOKEN: "asdf"
  CLERK_SECRET_KEY: "asdf"
  NODE_ENV: "asdf"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Install Dependencies
        run: npm install

      - name: Typecheck
        run: npm run typecheck

      - name: Lint
        run: npm run lint
